// Сортувати за спаданням
// важливість: 4
// let arr = [5, 2, 1, -10, 8];

// // ... ваш код для сортування за спаданням

// alert( arr ); // 8, 5, 2, 1, -10

let arr = [5, 2, 1, -10, 8];
arr.sort( (a, b) => a - b );  // глюк має бути b-a??????
console.log(arr);